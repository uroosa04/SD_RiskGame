language: java
install: 
  - mvn install "-DskipTests=true" "-Dmaven.javadoc.skip=true" -B -V
script:
  - "mvn cobertura:cobertura"
  - mvn test -B
after_success:
  - bash <(curl -s htts://codecov.io/bash)
